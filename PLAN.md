# PLAN — u2b-ukan (ERP-ядро)

> Читать ПЕРВЫМ в каждой сессии. Обновлять после каждого значимого шага.
> Метод построения — `U:\METHOD.md` (слои Controller→dto→service→repository→db).
> Схема БД — `docs/SCHEMA.md`.

## Цель
Новое ядро ERP по образцу 1С:УНФ, но в своей модели и визуализации: цепочка
документов (закуп → приходные накладные по поставщикам → доставка → расходная →
финанс/дашборд), двойная проводка, долги/оплаты, склады, производство. Мульти-орг
(мы + 2 филиала: производитель+продавец и чистый продавец). Расчёты SQL-агрегатами.

## Стек
Next.js 14 (App Router, `--src-dir`) + TS + Tailwind + Neon Postgres (pooler) +
Drizzle ORM + Zod. Деплой Vercel (Root Directory = `nextjs`). Репо публичный
`github.com/ulan1988/u2b-ukan` — секреты только в `.env.local` (в .gitignore).

## Статус
- ✅ Репо + `nextjs/`, Drizzle/Zod, схема БД (12 таблиц) в Neon (`db:push`).
- ✅ `.env.local` с DATABASE_URL (локально, не в гит), `.gitignore` закрывает секреты.
- ✅ **Документ «Приход» (закуп) — готов и проверен на Neon:**
  - Слои: `dto/document.dto.ts` (zod) → `services/document.service.ts` (проводка) →
    `repositories/{document,refs}.repo.ts` → `db`. `lib/num.ts` (номера ЗП/ПР).
  - Проводка атомарна (`db.batch`): documents + document_lines + stock_movements.
  - Роуты: `POST/GET /api/documents`, `GET /api/refs`, `POST /api/seed`.
  - Страница `/documents` (форма + список), главная → редирект на /documents.
  - Smoke ✅: seed + приход ЗП-0001 на 210000 создан, прочитан. tsc+build зелёные.
- Старт-данные засеяны (org «U2B головной», Центр-Склад, 2 поставщика, 3 товара).
  Тестовый приход ЗП-0001 — демо, можно удалить.

## Следующие шаги
1. ✅ **Задеплоено на Vercel** (Root Directory = `nextjs`, env DATABASE_URL) — приход создаётся на проде.
   Важно на будущее: при импорте выбирать папку `nextjs` (там значок Next.js); ветка `master`.
2. ✅ **Документ «Расход» (продажа)** — готов и проверен на Neon:
   - dto createSaleSchema, service createSale (проводка + `stock_movement −`), repo stockByWarehouse (остаток).
   - Роуты `POST/GET /api/sales`, `GET /api/stock`, клиенты в `/api/refs`. Страница `/sales` (форма + остаток + список).
   - Навигация в layout (Приход | Расход). Seed дополнен клиентами.
   - Smoke ✅: продажа ПР-0001 на 25000, склад Труба 95→85 (списание). tsc+build зелёные.
   - ⏳ Осталось (Фаза 2): `doc_link` на приход (себестоимость/маржа, FIFO).
3. ✅ **Оплаты** (`payment` in/out): dto/repo/service, `POST/GET /api/payments`, страница `/payments`. Касса в seed.
4. ✅ **Финанс-дашборд** `/finance`: дебиторка/кредиторка/касса/склад + баланс по контрагентам, всё SQL-агрегатами (`finance.repo` raw SQL). Smoke ✅: оплата снижает долг, повышает кассу.
5. ✅ **Себестоимость/маржа (рентабельность как в 1С)** — FIFO: createSale связывает продажу с партиями закупа (`doc_link`); отчёт `/profit` (выручка−себест=прибыль, маржа %), себест из связей + fallback priceIn. Smoke ✅ (ПР-0003: 50% маржа). Навигация: Рентабельность.
6. ✅ **Справочники из UI** (`/catalog`): номенклатура (с группами как в ulkan: Водосток/Материалы/Евробрус/Комплектующие… + категория товар/материал/услуга + 3 цены), контрагенты (тип/цена/телефон), склады, кассы. Создание работает (POST /api/{products,contragents,warehouses,cash-accounts}); lib/catalog.ts (группы). Smoke ✅.

7. ✅ **Удаление документов** (бывш. «сторно») — POST /api/documents/[id]/cancel: удаляет движения + связи, статус cancelled. Кнопка «🗑 Удалить», удалённые серым «Удалён». Smoke ✅.
8. ✅ **Возвраты** — return_in (от покупателя: +склад, −долг заказчика), return_out (поставщику: −склад, −наш долг). service.createReturn, POST/GET /api/returns?kind=in|out, страница /returns (тумблер типа), финансы учитывают возвраты (дебиторка−=ret_in, кредиторка−=ret_out). Номера ВП/ВС. Smoke ✅: склад и долги двигаются верно.

9. ✅ **Мульти-орг + 2 филиала** — 3 орг (головной hq + производитель + продавец). lib/org.ts (pickOrg/forOrg/localStorage), компонент OrgSwitcher в шапке (переключение + reload). Все экраны скоупятся по выбранной орг (документы/финансы по orgId, справочники-дропдауны filtered forOrg). Seed дополнен филиалами. Smoke ✅: 3 орг.

## Осталось для «завершения проекта»
- **Производство** (документ материалы→товар + размерное ценообразование см·м²·сумма для филиала-производителя). ← следующий
- **Вход/пользователи** (сейчас доступ открыт).
- **Редактирование справочников** (архив/правка), продажа между орг (филиалам).
- **Начальные остатки** — В КОНЦЕ (день перехода с 1С): импорт из 1С Excel в `opening_balance`. Владелец: пока рано, проект не закончен.
После каждого шага: `tsc --noEmit` → `build` → smoke → commit → push → прод.
